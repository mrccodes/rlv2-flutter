import 'package:rlv2_flutter/features/theme/theme_model.dart';

class ThemeRepository {
  Future<String> fetchUserTheme() async {
    // Fetch theme from user settings service/API
    return 'system'; // Replace with actual logic
  }

  Future<void> saveUserTheme(ThemeModeType themeMode) async {
    // Save theme to user settings service/API
  }
}
